# 📱 TechB NShop Rank 다중 서버(분산 노드) 구축 & 실기기 배포 매뉴얼

본 매뉴얼은 메인 서버(중앙 큐: `114.207.112.172:9003`)와 연동하여 **신규 PC/서버(2~3곳 이상)에 동일한 순위 수집 노드를 구축하고, 안드로이드 실기기(루팅폰)를 1대 이상 연결하여 자율 분산 구동**하기 위한 표준 실전 가이드입니다.

---

## 🏗️ 1. 분산 아키텍처 개요

```
                   [중앙 마스터 서버 (114.207.112.172)]
                     - Task Queue (Port 9003)
                     - 중앙 5분 독점 락(Lease) & 1-2-3단계 자동 인계
                                   │
      ┌────────────────────────────┼────────────────────────────┐
      ▼                            ▼                            ▼
[메인 서버 (현재)]           [신규 분산 노드 A]           [신규 분산 노드 B]
 - 고정 IP A                  - 신규 IP B                  - 신규 IP C
 - 보드 전용 폰팜 (16대)       - 루팅폰 1~3대 연결           - 루팅폰 N대 or PC 단독
 - Stage 3 모바일 + Stage 2   - Stage 3 모바일 자동 가동   - Stage 3 or Stage 2
```

- **중앙 제어**: 모든 노드는 중앙 큐 서버(`114.207.112.172:9003`)에서 작업을 가져오고 결과를 제출합니다.
- **IP 분산 효과**: 각 서버/폰의 독립된 네트워크(유선 IP, LTE 테더링 IP)로 요청이 분산되므로 네이버 차단 리스크가 극소화됩니다.
- **무간섭 자율성**: 신규 서버를 켜면 즉시 작업을 분담하고, 끄더라도 중앙 서버가 5분 후 다른 노드로 작업을 자동 재할당하므로 안전합니다.

---

## 📋 2. 준비물 및 사전 확인

1. **PC/서버 OS**: Ubuntu Desktop 22.04 LTS 또는 24.04 LTS (GUI 데스크톱 환경)
2. **안드로이드 기기**: 루팅된 안드로이드 스마트폰 (1대 이상) + 데이터 전송용 정품 USB 케이블 + 유전원 USB 허브
3. **네트워크**: 인터넷 연결 (유선 LAN 또는 Wi-Fi / 스마트폰 LTE 테더링)

---

## 📲 3. 루팅폰 사전 설정 (최초 1회)

스마트폰에서 다음 설정을 확인합니다:

1. **개발자 옵션 활성화**:
   - `설정` ➜ `휴대전화 정보` ➜ `소프트웨어 정보` ➜ **`빌드번호`를 7번 연속 터치**
2. **USB 디버깅 켜기**:
   - `설정` ➜ `개발자 옵션` ➜ **`USB 디버깅` 활성화 (ON)**
3. **화면 켜짐 유지**:
   - `설정` ➜ `개발자 옵션` ➜ **`충전 중 화면 켜짐 유지` 활성화 (ON)**
4. **기본 앱 확인**:
   - Google Chrome 브라우저가 설치되어 있고 최신 버전인지 확인합니다.

---

## 🚀 4. 신규 서버 원클릭 설치 절차 (Step-by-Step)

### Step 1: 소스코드 복사
메인 서버에서 코드를 복사하거나 배포 압축 파일을 신규 서버의 홈 디렉토리(`~/real_browser_nshop_rank`)에 압축 해제합니다.

```bash
cd ~
# 압축 파일 해제 예시
tar -xzf real_browser_nshop_rank.tar.gz
cd real_browser_nshop_rank
```

### Step 2: 원클릭 자동 환경 설정 스크립트 실행
Chrome 브라우저, ADB, Python 가상환경, 필수 라이브러리, 한글 폰트, 화면 절전 방지 설정을 자동으로 완료합니다.

```bash
chmod +x setup.sh start_worker.sh
./setup.sh
```
> ⏱️ 약 2~3분 소요되며, 모든 패키지와 Android USB udev 권한 설정이 완료됩니다.

---

## 🔌 5. 루팅폰 연결 및 USB 디버깅 승인

1. 스마트폰을 USB 케이블로 신규 PC에 연결합니다.
2. 스마트폰 화면에 **"USB 디버깅을 허용하시겠습니까?"** 팝업이 나타납니다.
3. ⭐️ **"이 컴퓨터에서 항상 허용"에 체크(V)**를 누른 후 **[허용]**을 터치합니다.
4. 신규 서버 터미널에서 정상 인식 여부를 확인합니다:

```bash
adb devices
```

**[정상 출력 예시]**
```
List of devices attached
R5CR11DKGPL    device
R3CNC0CY5FZ    device
```
> ⚠️ 만약 `unauthorized`로 뜬다면 폰 화면의 팝업에서 [허용]을 눌러야 합니다.

---

## 📶 6. 수십대 루팅폰 Wi-Fi 일괄 자동 설정 (OTG 없이 Wi-Fi 연결)

OTG 이더넷 젠더를 사용하지 않고 Wi-Fi로 통신할 때, 연결된 수십 대의 스마트폰에 일일이 Wi-Fi 비밀번호를 입력할 필요 없이 **명령어 1줄로 모든 기기에 병렬 자동 설정 및 인터넷 검증**을 완료합니다.

### 🌟 방법 1: 원클릭 쉘 스크립트 실행
```bash
# 사용법: ./setup_wifi.sh [와이파이이름] [비밀번호]
./setup_wifi.sh "Tech_5G" "13241324"

# 또는 인자 없이 실행 시 터미널에서 대화형으로 입력 가능
./setup_wifi.sh
```

### 🌟 방법 2: 파이썬 도구 상세 옵션 실행
```bash
# 10대 동시 병렬 연결 및 강제 재연결(-f)
./venv/bin/python3 scripts/setup_wifi.py --ssid "MyOffice_5G" --password "MyPassword123" --force
```

#### 📊 실행 결과 화면 예시 (16대 20초 만에 완료):
```
================================================================================
📶 [TechB WiFi Manager] 안드로이드 실기기 Wi-Fi 일괄 설정 시작
• 대상 SSID     : Tech_5G
• 비밀번호 설정 : [설정됨]
================================================================================
📱 총 16대의 기기가 감지되었습니다. 병렬 Wi-Fi 설정을 시작합니다...

  🟢 [R3CNC0CY5FZ] SM_G991U1 -> 연결 성공 (IP: 192.168.0.110, 인터넷: OK)
  🟢 [R5CR11DKGPL] SM_G991U  -> 연결 성공 (IP: 192.168.0.115, 인터넷: OK)
  ...
================================================================================
🎉 총 16대 중 16대 인터넷 연결 완료 (소요시간: 22.75초) | 부분연결: 0대 | 실패: 0대
================================================================================
```
- **동작 원리**:
  1. `root (su)` 권한으로 백그라운드 Wi-Fi 네트워크 직접 등록 및 승인.
  2. 만약 기기 보안 정책에 의해 백그라운드 등록이 막힐 경우, 자동으로 설정 화면을 열어 해당 SSID를 클릭하고 비밀번호를 입력하는 `UI Automator` 2차 폴백 작동.
  3. 기기별로 할당된 `wlan0` 사설 IP를 확인하고 `8.8.8.8` Ping 테스트를 수행하여 실제 인터넷 통신 가능 여부를 전수 검증.

---

## 🏃 7. 워커 구동 방법 (`start_worker.sh`)

신규 서버는 PM2 데몬 대신 터미널에서 **단일 쉘 스크립트(`start_worker.sh`)**로 구동합니다.

### 🌟 방법 A: 스마트 기기 자동 감지 실행 (가장 추천!)
인자 없이 그냥 실행하면 연결된 폰 대수를 자동 파악하여 최적 모드로 시작합니다:

```bash
./start_worker.sh
```
- **폰이 1대 연결된 경우** ➜ `Stage 3 (실기기 모드, 1쓰레드)` 자동 가동
- **폰이 3대 연결된 경우** ➜ `Stage 3 (실기기 모드, 3쓰레드)` 자동 가동
- **폰이 연결되지 않은 경우** ➜ `Stage 2 (PC 브라우저 모드, 4쓰레드)` 자동 전환

---

### 🌟 방법 B: 백그라운드 상시 구동 (터미널 창을 닫아도 계속 유지)
SSH 세션이 끊기거나 터미널 창을 닫아도 백그라운드에서 24시간 계속 돌리려면 `nohup`을 사용합니다:

```bash
# 백그라운드 실행
nohup ./start_worker.sh > worker.log 2>&1 &

# 실시간 로그 확인
tail -f worker.log
```

---

### 🌟 방법 C: 수동 지정 실행 (원하는 쓰레드/스테이지 강제)
```bash
# 문법: ./start_worker.sh [쓰레드수] [스테이지: 2=PC, 3=모바일]

# 예시 1: 폰 2대로 실기기 3단계 구동
./start_worker.sh 2 3

# 예시 2: PC 전용 브라우저로 4쓰레드 2단계 구동
./start_worker.sh 4 2
```

---

## 📊 8. 실시간 모니터링 및 프로세스 종료

### 1) 실행 상태 및 캐시 적중 현황 확인
```bash
# 실행 중인 워커 프로세스 확인
ps aux | grep -E "python3.*main.py"

# 실기기(Stage 3) 실시간 수집 및 캐시 현황 확인
python3 -c '
import json
with open("services/runtime/logs/drain_status_stage3.json") as f:
    d = json.load(f)
print(f"진행상황: {d.get(\"total_processed\")}건 처리 완료 | 잔여: {d.get(\"total_remaining_tasks\")}개 | 성공률: {d.get(\"success_rate\")}% | 캐시적중률: {d.get(\"cache_hit_rate\")}%")
'
```

### 2) 워커 종료 (Stop)
```bash
# 실행 중인 모든 워커 안전 종료
pkill -f "main.py worker"

# 필요 시 크롬/ADB 정리
pkill -f "/usr/bin/google-chrome"
pkill -f "/opt/google/chrome/chrome"
```

---

## ❓ 9. 자주 묻는 질문 및 트러블슈팅 (FAQ)

### Q1. `adb devices`에 기기가 `unauthorized`로 나옵니다.
- **원인**: 폰 화면에서 USB 디버깅 승인을 누르지 않았습니다.
- **해결**: 스마트폰 화면을 켜고 "이 컴퓨터에서 항상 허용"에 체크 후 [허용]을 누르세요. 만약 팝업이 안 뜨면 USB 케이블을 뺐다 다시 꽂으세요.

### Q2. 폰 화면이 자꾸 꺼지거나 잠깁니다.
- `start_worker.sh`가 구동되면 기기 할당 시 자동으로 `svc power stayon true`(화면 상시 켜짐) 및 `wm dismiss-keyguard`(잠금 해제) 명령을 전송합니다. 루팅폰의 경우 `설정` ➜ `개발자 옵션` ➜ `충전 중 화면 켜짐 유지`를 켜두시면 더욱 안전합니다.

### Q3. 중앙 큐 서버 주소를 변경해야 하나요?
- 기본값으로 `config/settings.py`에 메인 서버 주소(`http://114.207.112.172:9003`)가 내장되어 있으므로 **설정을 전혀 변경할 필요가 없습니다**.
- 만약 환경변수로 변경하고 싶다면 `export TASK_QUEUE_SERVER="http://신규IP:9003"`으로 오버라이드할 수 있습니다.

### Q4. 신규 서버에서도 이전 캐시를 재활용할 수 있나요?
- 신규 서버는 자체적으로 당일 11시/19시 세션 폴더에 실시간으로 캐시를 누적하여 동일 검색어가 들어올 때 0.0001초 만에 재활용합니다.
- 메인 서버의 오늘자 캐시(`2026-09-06/11`)를 신규 서버의 `services/runtime/keyword_cache/` 폴더에 복사해 두면 신규 서버에서도 첫 번째 실행부터 즉시 캐시 적중(0초 반환) 효과를 볼 수 있습니다.
